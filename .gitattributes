# Normalise to LF in the repo. Without this, the deploy scripts get CRLF on a
# Windows checkout and fail on the Ubuntu server with "bad interpreter".
* text=auto eol=lf

*.png binary
*.ico binary
*.tar.gz binary
